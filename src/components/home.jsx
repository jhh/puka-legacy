import React, {PropTypes} from 'react';
import SearchableBookmarkListContainer from '../containers/searchable-bookmark-list-container'

export const Home = (props) => {
  return (<SearchableBookmarkListContainer />)
}

Home.propTypes = {
};
