#!/bin/sh
. config/scripts.sh
webpack-dev-server                       \
  --config $CONFIG_DIR/webpack.config.js \
  --hot                                  \
  --inline                               \
  --content-base "output"                \
  --history-api-fallback
